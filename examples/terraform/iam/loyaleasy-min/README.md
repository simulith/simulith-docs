# loyaleasy-min IAM on Simulith

RDS Proxy IAM role + policy subset from `proxydb/proxydb.tf`.

## Apply

```bash
cd runtime/examples/terraform/iam/loyaleasy-min
terraform init
terraform apply
```

## Destroy

```bash
terraform destroy
```
