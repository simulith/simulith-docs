# proxy-roles-min IAM on Simulith

RDS Proxy IAM role + policy subset for local Terraform validation.

## Apply

```bash
cd runtime/examples/terraform/iam/proxy-roles-min
terraform init
terraform apply
```

## Destroy

```bash
terraform destroy
```
